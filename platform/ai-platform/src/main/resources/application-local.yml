spring:
  config:
    activate:
      on-profile: local

  datasource:
    url: ${AI_DB_URL:jdbc:postgresql://localhost:5432/ai_platform?currentSchema=public&sessionVariables=timezone%3DUTC}
    username: ${AI_DB_USERNAME:postgres}
    password: ${AI_DB_PASSWORD:postgres}
    driver-class-name: org.postgresql.Driver

  jpa:
    show-sql: true
    properties:
      hibernate:
        jdbc:
          time_zone: UTC
