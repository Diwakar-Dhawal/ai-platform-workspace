spring:
  config:
    activate:
      on-profile: local

  datasource:
    url: ${CHAT_DB_URL:jdbc:postgresql://localhost:5432/chat_service?currentSchema=public&sessionVariables=timezone%3DUTC}
    username: ${CHAT_DB_USERNAME:postgres}
    password: ${CHAT_DB_PASSWORD:postgres}
    driver-class-name: org.postgresql.Driver

  jpa:
    show-sql: true
    properties:
      hibernate:
        jdbc:
          time_zone: UTC
