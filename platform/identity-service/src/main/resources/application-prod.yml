spring:
  config:
    activate:
      on-profile: prod

  datasource:
    url: ${IDENTITY_DB_URL}
    username: ${IDENTITY_DB_USERNAME}
    password: ${IDENTITY_DB_PASSWORD}

  jpa:
    show-sql: false

jwt:
  secret: ${IDENTITY_JWT_SECRET}
